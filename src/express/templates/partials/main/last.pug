section.main-page__last.last
  h2.last__name
    | Последние комментарии&nbsp;
    span.last__icon.last__icon--cloud
  if lastComments.length
    ul.last__list
      each comment, index in lastComments
        li.last__list-item(class={"last__list-item--end": index === lastComments.length - 1})
          img.last__list-image(src=`/img/${comment.avatar}` width="20" height="20" alt="Аватар пользователя")
          b.last__list-name #{comment.firstName} #{comment.lastName}
          a.last__list-link(href=`${AppRoute.ARTICLE}/${comment.articleId}#comment-${comment.id}`)
            +truncate-string(comment.text)
  else
    p.last__empty Здесь пока ничего нет...
