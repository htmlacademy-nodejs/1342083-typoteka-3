'use strict';

const StringSchemaAlias = {
  MIN: `string.min`,
  MAX: `string.max`,
  EMPTY: `string.empty`,
  EMAIL: `string.email`,
  PATTERN_BASE: `string.pattern.base`,
};

module.exports = {
  StringSchemaAlias,
};
