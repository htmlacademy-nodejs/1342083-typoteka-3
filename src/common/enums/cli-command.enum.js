'use strict';

const CliCommand = {
  FILL: `--fill`,
  FILLDB: `--filldb`,
  GENERATE: `--generate`,
  HELP: `--help`,
  SERVER: `--server`,
  VERSION: `--version`,
};

module.exports = {
  CliCommand,
};
