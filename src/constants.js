'use strict';

const ADDITIONAL_ARGV_SPLIT_INDEX = 2;
const DEFAULT_ARGV = `--help`;
const ExitCode = {
  SUCCESS: 0,
  ERROR: 0,
};

module.exports = {
  ADDITIONAL_ARGV_SPLIT_INDEX,
  DEFAULT_ARGV,
  ExitCode,
};
