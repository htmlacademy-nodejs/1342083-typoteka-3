'use strict';

const ADDITIONAL_ARGV_SPLIT_INDEX = 2;
const ExitCode = {
  ERROR: 1,
  SUCCESS: 0,
};
const RANDOM_SEPARATOR = 0.5;

module.exports = {
  ADDITIONAL_ARGV_SPLIT_INDEX,
  ExitCode,
  RANDOM_SEPARATOR,
};
