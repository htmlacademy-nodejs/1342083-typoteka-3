section.main-page__last.last
  h2.last__name
    | Последние комментарии&nbsp;
    span.last__icon.last__icon--cloud
  if lastComments.length
    ul.last__list
      each comment, index in lastComments
        li.last__list-item(class={"last__list-item--end": index === lastComments.length - 1})
          img.last__list-image(src="/img/avatar-2.png" width="20" height="20" alt="Аватар пользователя")
          b.last__list-name Анна Артамонова
          a.last__list-link(href=`${AppRoute.ARTICLES}/${comment[CommentKey.ARTICLE_ID]}#comment-${comment[CommentKey.ID]}`)
            | #{truncateString(comment[CommentKey.TEXT])}
  else
    p.last__empty Здесь пока ничего нет...
